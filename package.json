{
  "name": "extract-package-name",
  "version": "1.0.0",
  "description": "A command line tool that extracts the package name from a package.json file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
	"extract-package-name": "./index.js"
  },
  "author": "Dan Kolz",
  "license": "ISC",
  "dependencies": {
    "console-in-transformer": "^1.0.2"
  },
  "repository": "https://github.com/EmergentIdeas/extract-package-name.git"
}
